# apps/web environment

# Server-side only — used by Next.js route handlers when proxying to the
# NestJS API. NEVER expose to the browser. Phase 2.5 will pair this with
# a Clerk JWT in the Authorization header; for now the NestJS endpoints
# are dev-gated by NODE_ENV.
NEXT_INTERNAL_API_URL="http://localhost:3001"
